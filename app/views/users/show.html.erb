<% content_for :title, @user.name %>


<div class='row'>
  <section class='user_info'>
    <h1>
      <%= image_tag @user.picture.url if @user.picture? %>
      <%= @user.name %>
    </h1>
  </section>
</div>